import React from "react";
import ToggleSection from "./ToggleSection";

export default {
  title: "Core/ToggleSection",
  component: ToggleSection,
};

export const Default = (): React.ReactNode => {
  return <ToggleSection />;
};
